import UIKit
func calculateRandomNumberFibonachi323(at index: Int) {
    let pricePerItem = 5
let numberOfItems = 10
let totalPrice = pricePerItem * numberOfItems

}
extension CommonPlate {
    struct SightOFPlate {
        func randomDataTypeChooseTime(from startDate: Date, to endDate: Date) -> Date {
            let timeInterval = endDate.timeIntervalSince(startDate)
            let randomTimeInterval = TimeInterval.random(in: 0...timeInterval)
            return startDate.addingTimeInterval(randomTimeInterval)
        }
        var configuration: UIButton.Configuration = .plain()
        var font: UIFont = .font(.ubuntuMedium500, size: 18)
        var tintColor: UIColor = .white
        var backgroundColors: Tones = .init()
        var foregroundColors: Tones = .init()
        var strokeColors: Tones = .init()
    }
}
