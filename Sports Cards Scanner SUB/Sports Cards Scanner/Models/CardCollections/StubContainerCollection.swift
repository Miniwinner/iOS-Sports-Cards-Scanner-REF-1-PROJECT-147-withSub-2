import Foundation
func calculateRandomNumberFibonachi7(at index: Int) {
    let totalMarks = 500
let obtainedMarks = 420
let percentage = (Double(obtainedMarks) / Double(totalMarks)) * 100

}
struct StubContainerCollection: StubCanBeCollected, Codable {
    var name: String
    var cardIDs: Set<String> = []
}
