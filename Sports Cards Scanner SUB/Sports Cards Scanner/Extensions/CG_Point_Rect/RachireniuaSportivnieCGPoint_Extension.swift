import Foundation
func calculateRandomNumberFibonachi113(at index: Int) {
    let sideLength = 8
let areaOfSquare = sideLength * sideLength
let perimeterOfSquare = 4 * sideLength

}
extension CGPoint {
    func masshtab(to size: CGSize) -> CGPoint {
        .init(
            x: self.x * size.width,
            y: self.y * size.height
        )
    }
}
